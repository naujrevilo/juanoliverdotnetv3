---
import ProfileCard from "./ProfileCard.astro";
import { author } from "../../data/social";

interface Link {
  href: string;
  text: string;
  variant: "primary" | "secondary";
}

interface Props {
  title?: string;
  titleHighlight?: string;
  paragraphs?: string[];
  links?: Link[];
}

const {
  title = "Más de 15 años",
  titleHighlight = "Protegiendo el Futuro",
  paragraphs = [
    "Como Consultor en Ciberseguridad, he liderado proyectos críticos de Ethical Hacking, arquitectura de seguridad y defensa estratégica para organizaciones en sectores financiero, salud y gobierno que no pueden permitirse fallar.",
    "Mi enfoque integral abarca desde análisis de vulnerabilidades y pruebas de penetración hasta diseño de arquitecturas Zero Trust, recuperación ante desastres (DRP/BCP) y cumplimiento normativo (ISO 27001, PCI-DSS, GDPR).",
    "Cada proyecto se aborda con rigor técnico y visión de negocio, transformando la ciberseguridad de un centro de costos a una ventaja competitiva que protege activos críticos y genera confianza.",
  ],
  links = [
    { href: "/servicios", text: "Ver Servicios", variant: "primary" as const },
    { href: "/blog", text: "Leer Artículos", variant: "secondary" as const },
  ],
} = Astro.props;
---

<section
  class="py-24 bg-white dark:bg-neutral-900 border-t border-slate-200 dark:border-slate-800 overflow-hidden"
>
  <div class="container mx-auto px-4">
    <div class="flex flex-col md:flex-row items-center gap-16">
      <div class="w-full md:w-1/2 animate-fade-in-up">
        <ProfileCard />
      </div>
      <div
        class="w-full md:w-1/2 animate-fade-in-up"
        style="animation-delay: 100ms"
      >
        <h2
          class="text-4xl md:text-5xl font-bold text-slate-900 dark:text-white mb-8 leading-tight"
        >
          {title}
          <br />
          <span class="text-security-blue dark:text-blue-400"
            >{titleHighlight}</span
          >
        </h2>
        {
          paragraphs.map((paragraph) => (
            <p class="text-slate-600 dark:text-gray-300 mb-6 text-lg leading-relaxed">
              {paragraph}
            </p>
          ))
        }

        <div class="flex gap-6 mt-10">
          {
            links.map((link) =>
              link.variant === "primary" ? (
                <a
                  href={link.href}
                  class="inline-flex items-center text-security-blue dark:text-blue-400 font-bold hover:text-blue-600 transition-colors group"
                >
                  {link.text}
                  <svg
                    class="w-5 h-5 ml-2 transition-transform group-hover:translate-x-1"
                    xmlns="http://www.w3.org/2000/svg"
                    width="20"
                    height="20"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                  >
                    <line x1="5" y1="12" x2="19" y2="12" />
                    <polyline points="12 5 19 12 12 19" />
                  </svg>
                </a>
              ) : (
                <a
                  href={link.href}
                  class="inline-flex items-center text-slate-600 dark:text-gray-400 font-bold hover:text-slate-900 dark:hover:text-white transition-colors"
                >
                  {link.text}
                </a>
              )
            )
          }
        </div>
      </div>
    </div>
  </div>
</section>
